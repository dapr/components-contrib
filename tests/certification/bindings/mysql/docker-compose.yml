version: '2'
services:
  db:
    image: mysql:8
    command: --mysql_native_password=ON
    restart: always
    ports:
      - "3306:3306"
    environment:
      MYSQL_USER: mysql
      MYSQL_PASSWORD: example
      MYSQL_DATABASE: dapr_test
      MYSQL_ROOT_PASSWORD: root
